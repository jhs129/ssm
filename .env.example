# Managed automatically by the Vercel CLI when linked to the project (`vercel env pull`).
# Do not set this manually.
VERCEL_OIDC_TOKEN=

# Google Analytics 4 measurement ID. Safe to expose client-side.
NEXT_PUBLIC_GA_ID=
