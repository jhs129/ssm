{
  "name": "@repo/types",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'No linting configured for types package'"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
