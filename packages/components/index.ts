// @repo/components — shared Builder.io-registrable component library.
// Intentionally empty at this stage: scaffolded as part of a monorepo
// restructuring pass. ssm's existing marketing-site sections remain
// coded, app-local components in apps/web/components/ and are not
// ported here.
//
// Expected future shape (populated by the Builder.io content-integration
// work): registration/registry barrels plus individual components
// registered as Builder.io content blocks (starting with Button).
export {}
